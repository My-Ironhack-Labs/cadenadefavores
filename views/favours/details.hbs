<div class="container">
  <div class="row justify-content-center">
    <h2> Detalles Favores</h2>
   
    <hr>
    <div class="col-md-12">
      <ul>
        <li class="favourList">
          <p>{{title}}</p>
          <p>{{description}}</p>
          <p>{{address}}</p>

          <form method="POST" action="/favores/detalles/{{ _id }}">
            <div class="form-group">
              <label for="status">Status: {{status}}</label>
              <input name="status" type="text" class="form-control d-none" id="status" value="required" readonly>
            </div>
            <div class="form-group">
              <label for="started">started: {{started}}</label>
              <input name="started" type="text" class="form-control d-none" id="started" value="true" readonly>
            </div>
            <button id="hacerFav" type="submit" class="btn btn-success">Hacer favor</button>
          </form>

          {{#if started}}
          <form method="POST" action="/favores/detalles/terminar/{{ _id }}">
            <div class="form-group">
              <label for="status">Status: {{status}}</label>
              <input name="status" type="text" class="form-control d-none" id="status" value="done" readonly>
            </div>
            <button id="hacerFav" type="submit" class="btn btn-success">Finalizar favor</button>
          </form>
          {{/if}}
          <a class="btn btn-success" href="/favores/editar/{{_id}}">Editar</a>
          <a href="/favores" class="btn btn-success">Favores</a>
          <form method="POST" action="/favores/eliminar/{{_id}}">
            <button type="submit" class="btn btn-success">Borrar</button>
          </form>
          <hr>
        </li>
      </ul>
    </div>
  </div>